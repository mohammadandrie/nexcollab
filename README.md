# Nexcollab

Ready-to-run prototype for the Nexcollab team workspace.

## Run locally

Open `index.html` directly in a browser, or serve it:

```bash
python3 -m http.server8080
```

Then visit http://localhost:8080

## Files
- `index.html` — main UI mockup
- `docs.html` — docs / notes view
